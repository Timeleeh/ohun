fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0
# LLM providers (둘 중 사용하는 것만 있으면 됨)
anthropic>=0.111
google-genai>=1.0
# DB (운영용; 없으면 인메모리 store로 동작)
supabase>=2.0
# 공유 카드 SSR(PNG 렌더링)
Pillow>=10.0
