# Claude API
ANTHROPIC_API_KEY=sk-ant-xxxxx
# 운세 문장 생성에 사용할 모델 (위트·다양성용. 일 배치라 비용/속도 균형상 sonnet 권장)
OHN_MODEL=claude-sonnet-4-6
# 위트/다양성 확보용. tool use로 JSON 구조를 강제하므로 높게 써도 안전
OHN_TEMPERATURE=0.95
# 그룹 최대 인원 (기획 확정값)
OHN_MAX_GROUP_SIZE=6
# LLM에 전달할 케미 페어 상위 개수 (상생/상극 드라마 강한 순)
OHN_MAX_PAIRS=6
